import { createSlice } from '@reduxjs/toolkit'

const eventSlice = createSlice({
    name: 'eventSlice',
    initialState: [],
    reducers: {
        // reducer functions go here
    }
})

// export reducer functions here
// export const {} = slice.actions
export default eventSlice.reducer
